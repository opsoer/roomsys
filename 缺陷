目前测试发现下面的问题：
1、房间的出租流程还需补充一个流程；1、先缴纳定金，前端提供一个 设为被预定 的按钮；被预定需要填写
房间变为已被预定的状态，公寓页面就不展示了；












下面的不要管，读到这里就行了
#3、基于当前生效租约自动计算未来应收租金；目前测试没有这个能力，我想要通过所有的租约情况去预测目前为止的未来几个月的租金收益
#4、清理目前的所有的数据，包括数据的数据以及重新创建表，清理房间的照片和视频
#通过目前租约计算未来收益、大致就是

1、提供一些测试的简单接口和web界面，比如可以在管理员管理里面通过修改当前系统时间的接口，
用于测试，这样我就可以观察房间状态、账单、分红等变化；
2、写一个脚本，注入大量的数据的脚本



记录所有的分账记录，比如几号谁谁股东分了多少钱，详细备注
图片视频压缩再存入对象存储




搞个小窗口引入个人业主的小产权

如果可以，做好分类，比如拎包入住 公寓、基础家店 民房、空房


营销：改变中介行业、让中介当销售

收入：
考虑引入广告、房东积分评比、卖权限、
租客端限制获取房东电话次数或者是浏览房间个数


- ✅ 对象存储：七牛云 + 公开桶 + CDN
- ✅ 防盗链：七牛 CDN Referer + UA 黑名单
- ✅ 图片压缩：后端 Go 标准库
- ✅ 视频压缩：后端调 FFmpeg（需确认你服务器有没有 FFmpeg）
- ✅ 监控：Umami + 复用 MySQL
- ✅ 部署：Docker Compose 三件套（Nginx + 你后端 + Umami）


FFmpeg 不是 Go 依赖包，它是一个独立的命令行工具（可执行文件），就像 git、npm 一样装到操作系统上。Go 程序通过 os/exec 调用它，相当于在代码里执行了一个脚本命令。
安装有多麻烦
Windows 开发机：去 https://ffmpeg.org/download.html 下载 zip → 解压 → 把 bin/ 目录加到 PATH 环境变量 → 重启终端 → 搞定（5 分钟）
Linux 服务器：一行命令搞定
apt install ffmpeg   # Ubuntu/Debian
yum install ffmpeg   # CentOS
数据流程（改装后）
手机上传 200MB 视频
       │
       ▼
Go 收到文件 → 写入临时文件（tmp/original_xxx.mp4）
       │
       ▼
Go 执行: ffmpeg -i tmp.mp4 -b:v 2M -vf scale=-2:720 compressed.mp4
       │                          ↑ 转 720p 2Mbps ≈ 压缩到 20-30MB
       ▼
Go 读到 compressed.mp4 → 上传到七牛 → 删除两个临时文件
       │Plan·DeepSeek V4 Flash FreeOpenCode Zen
       ▼
DB 记录 file_path = videos/compressed_xxx.mp4（存的是压缩版）
全程后端异步处理，用户上传界面正常显示进度条。上传时间会变长（多 10-30 秒压缩时间），但上传的数据量从 200MB 降到 20-30MB，整体等待时间其实是缩短的。