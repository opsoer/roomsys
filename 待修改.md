# 待修改清单

> 扫描时间：2026-07-02
> 最后更新：2026-07-03
> 覆盖范围：后端 server/ + 前端 web/ 全部源码
> 问题总数：106 个（高危 18 个、中危 63 个、低危 25 个）
> 已修复：100 个（高危 17 个、中危 60 个、低危 23 个）
> 未修复：6 个（高危 1 个、中危 4 个、低危 1 个）

---

## 一、高危问题（18 个）

### 1.1 安全 - 敏感信息硬编码 ✅
- **后端** `server/config.json` — 数据库密码、JWT 密钥、七牛云 AK/SK 明文写入配置文件
- **修复**：config.json 替换为占位符，创建 `.env.example` 模板，敏感信息通过环境变量注入

### 1.2 安全 - 默认管理员弱密码 ✅
- **后端** `server/main.go` / `server/scripts/migrate.go` — 默认密码 `admin123`
- **修复**：首次创建时生成 16 位随机密码，输出到日志，脚本同步修复

### 1.3 安全 - CSRF 保护形同虚设 ✅
- **后端** `server/main.go` — 仅检查 `X-Requested-With` 头非空
- **修复**：改为校验 `X-CSRF-Token` 头且长度 >= 16

### 1.4 安全 - 公开页面泄露租客隐私
- **前端** `web/src/views/BuildingPublicRoom.vue:98-103` — 未登录即可看到租客姓名和手机号
- **状态**：已修复（之前轮次）

### 1.5 安全 - 公开页面泄露房东手机号
- **前端** `web/src/views/BuildingPublic.vue:47` — 直接显示房东完整手机号
- **状态**：已修复（之前轮次）

### 1.6 安全 - Token 存 localStorage 易被 XSS 窃取
- **前端** `web/src/stores/auth.js:5-9` — 所有认证信息存在 localStorage
- **状态**：未修复（架构变更较大，需 httpOnly Cookie 方案）

### 1.7 业务 - 租金账单按天计算金额错误
- **后端** `server/handlers/rent.go` — 按当前日期天数比例计算金额
- **状态**：已修复（之前轮次）

### 1.8 业务 - 公寓过期判断使用硬编码日期
- **后端** `server/services/building.go` — 状态过滤使用固定日期
- **状态**：已修复（之前轮次）

### 1.9 业务 - 退租处理未结束合同
- **后端** `server/handlers/tasks.go` — 退租只更新房间状态
- **状态**：已修复（之前轮次）

### 1.10 业务 - 退租时未清理旧合同
- **后端** `server/handlers/rooms.go` — 新签合同时未检查/结束旧活跃合同
- **状态**：已修复（之前轮次）

### 1.11 业务 - 复制登录链接指向不存在的路由
- **前端** `web/src/views/AdminBuildings.vue` / `AdminBuildingDialogs.vue`
- **状态**：已修复（之前轮次）

### 1.12 业务 - 房间筛选条件前后端不一致 ✅
- **前端** `web/src/views/BuildingPublic.vue:155-158`
- **修复**：后端 GetRooms 添加 status/floor/layout 过滤参数，前端改为服务端筛选

### 1.13 错误处理 - 多处数据库查询错误被静默忽略 ✅
- **后端** 多个 Handler 的 List 方法 — 查询失败返回 200
- **修复**：所有 List 方法查询失败返回 500；GetShareholders 错误不再静默忽略

### 1.14 并发 - 账单编号生成存在竞态 ✅
- **后端** `server/utils/time.go` + `server/services/bill.go` — 两套生成机制混用
- **修复**：移除 utils.GenerateBillNo，统一使用 services 层的 building-scoped COUNT 方式；tasks.go 同步修复

### 1.15 性能 - Building List N+1 查询
- **后端** `server/services/building.go:79-92` — 每个 building 执行 4 条 COUNT
- **状态**：已修复（之前轮次）

### 1.16 数据验证 - 金额字段无非负校验
- **后端** `server/handlers/bills.go` / `server/handlers/rooms.go`
- **状态**：已修复（之前轮次）

### 1.17 数据库 - AutoMigrate 在生产环境运行 ✅
- **后端** `server/main.go:48-53` — 所有环境都执行 AutoMigrate
- **修复**：生产环境（GIN_MODE=release）跳过 AutoMigrate

### 1.18 代码重复 - mediaUrl 函数在 8 个文件重复定义
- **前端** 8 个文件中重复定义 `mediaUrl`
- **状态**：已修复（之前轮次）

---

## 二、中危问题（63 个）

### 2.1 安全（6 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | JWT 刷新令牌无吊销机制 | ✅ 已修复：添加内存黑名单 + CleanupRevokedTokens |
| 2 | CORS 源硬编码为 localhost 开发地址 | ✅ 已修复：支持 CORS_ORIGINS 环境变量配置 |
| 3 | 缺少 CSP、HSTS、Permissions-Policy 安全头 | 已修复（之前轮次） |
| 4 | 除登录外所有接口无速率限制 | ✅ 已修复：全局 RateLimit 中间件 60 req/min |
| 5 | 上传目录权限 0755 应改为 0750 | 已修复（之前轮次） |
| 6 | 路由守卫直接读 localStorage | 已修复（之前轮次） |

### 2.3 错误处理（5 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | strconv.ParseUint 错误被忽略 | 已修复（之前轮次） |
| 2 | handleDepositRefund 返回值被忽略 | ✅ 已修复：检查错误并回滚事务 |
| 3 | 日志文件打开失败未阻止启动 | ✅ 已修复：改为仅输出警告，继续启动 |
| 4 | 401 处理直接操作 localStorage | 已修复（之前轮次） |
| 5 | 响应数据解包逻辑可能产生意外行为 | ✅ 已修复：增加 null 检查 |

### 2.4 并发（2 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 限流器 Mutex + sync.Map 混用，cleanup 存在竞态 | ✅ 已修复：统一使用 Mutex + map |
| 2 | 定时任务 goroutine 无 recover 保护 | 已修复（之前轮次） |

### 2.5 架构（3 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | Service 对象在三个路由组中重复创建 | ✅ 已修复：提取到顶部共享实例 |
| 2 | 日期字段混用 string 和 time.Time 类型 | 未修复 |
| 3 | Handler 层直接操作数据库，绕过 Service 层 | 未修复 |

### 2.6 性能（4 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 月度统计每月 2 条 SQL，12 个月执行 24 条 | ✅ 已修复：GetTrend 改为单条 GROUP BY |
| 2 | 租金账单全表扫描 + 内存逐条检查 | ✅ 已修复：改用 JOIN 查询 |
| 3 | 到期检查全量加载到内存 | ✅ 已修复：LIMIT 100 |
| 4 | 30 秒轮询未用 Page Visibility API 优化 | ✅ 已确认：AdminLayout 已实现 |

### 2.8 API 设计（4 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 所有列表接口不支持分页 | ✅ 已修复：buildings/bills/rooms 列表添加 page/page_size 参数 |
| 2 | 删除房间不检查活跃合同 | ✅ 已修复：删除前检查 active 合同数量 |
| 3 | 缺少统一业务错误码体系 | ✅ 已修复：添加 CodeSuccess/CodeMonthSettled 等错误码 + ErrorWithCode |
| 4 | 30 秒超时不够大文件上传 | ✅ 已修复：uploadApi 300s 超时 |

### 2.9 数据库（3 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 软删除数据无定期清理机制 | ✅ 已修复：CleanupSoftDeleted 每日清理 90 天前数据 |
| 2 | 外键未配置 constraint:OnDelete:CASCADE | ✅ 已修复：RoomMedia/RentalContract.CASCADE, Bill.RoomID.SET NULL |
| 3 | 4 条独立 COUNT 可合并为一条聚合 SQL | ✅ 已修复：GetWithStats 改为单条聚合查询 |

### 2.10 代码重复（5 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | building_id 获取代码重复 | ✅ 已修复：全部 handler 统一使用 utils.GetBuildingID() |
| 2 | Upload 和 UploadCover 逻辑高度重复 | ✅ 已修复：提取 validateUploadFile + uploadToStorage 公共函数 |
| 3 | format.js vs useUtils.js 功能重复 | ✅ 已修复：useUtils.js 改为从 format.js 导入 |
| 4 | statusLabel/statusTagType 重复且不一致 | 已修复（之前轮次） |
| 5 | 楼层/户型选项硬编码重复 | ✅ 已修复：提取到 utils/constants.js |

### 2.11 前端状态管理（3 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | Store 未监听 storage 事件，多标签页不同步 | ✅ 已修复：添加 storage 事件监听 |
| 2 | 401 时未调用 authStore.logout() | 已修复（之前轮次） |
| 3 | 直接读 localStorage 而非使用 Store | 已修复（之前轮次） |

### 2.12 前端类型安全（2 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 无 TypeScript，props 无类型约束 | 未修复 |
| 2 | 空 catch 完全静默吞错 | ✅ 已修复：RoomDetail.vue catch 块添加错误日志 |

### 2.13 前端用户体验（1 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 加载失败只显示 toast 无重试按钮 | ✅ 已修复：BuildingPublic.vue 添加重试按钮和错误状态 |

---

## 三、低危问题（25 个）

### 后端（12 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | Districts 接口返回空数组（桩代码） | ✅ 已修复：BuildingList.vue 改为使用 shenzhen.js 本地数据 |
| 2 | 账单更新返回旧对象而非更新后版本 | 已修复（之前轮次） |
| 3 | 响应码使用 HTTP 状态码而非业务码 | 未修复 |
| 4 | 测试覆盖率极低 | 未修复 |
| 5 | 更新房间状态未拒绝无效状态值 | ✅ 已修复：添加 rented/vacant 校验 |
| 6 | seedAdmin 函数重复实现 | ✅ 已修复：scripts/migrate.go 同步更新 |
| 7 | 公开接口暴露合同详情 | 未修复（设计如此，仅认证用户可见） |
| 8 | 模拟时间接口无操作审计 | 未修复 |
| 9 | 数据库连接池配置不够精细 | 未修复（当前配置已合理） |
| 10 | 日期查询用 LIKE 无法利用索引 | ✅ 已修复：rent.go/utils/bill.go 改用日期范围查询 |
| 11 | 删除公寓级联不完整 | 已修复（之前轮次） |

### 前端（13 个）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 移动端检测逻辑重复 | ✅ 已修复：提取 useMobile composable |
| 2 | 移动端用户菜单实现重复 | ✅ 已修复：提取 MobileUserMenu 公共组件 |
| 3 | 模板大量使用内联样式 | 未修复 |
| 4 | 图标全局注册与按需导入不一致 | ✅ 已修复：移除 9 个文件的冗余本地导入 |
| 5 | 上传组件同时设置 action 和 http-request | ✅ 已修复：移除冗余 action 属性 |
| 6 | 上传响应数据访问路径不确定 | ✅ 已修复：统一使用 res.data?.cover_image |
| 7 | catch 块静默吞掉删除错误 | ✅ 已修复：添加错误日志和用户提示 |
| 8 | 账单金额 min=0 允许零金额 | ✅ 已修复：改为 min=0.01 |
| 9 | 手机号正则过于宽松 | ✅ 已修复：改为 /^1[3-9]\d{9}$/ |
| 10 | ECharts 重复注册 | ✅ 已修复：提取到 utils/echarts.js 统一注册 |
| 11 | 登录页使用 Vant 组件，桌面端视觉不佳 | 未修复 |
| 12 | 缺少 ESLint 配置 | 未修复 |
| 13 | 测试覆盖率极低 | 未修复 |

---

## 四、已修复问题汇总（84 个）

### 后端已修复（37 个）
1. **1.1** - config.json 替换为占位符，创建 .env.example
2. **1.2** - main.go/migrate.go 生成随机管理员密码
3. **1.3** - main.go CSRF 改为校验 X-CSRF-Token
4. **1.7** - rent.go 改为按月生成固定金额账单
5. **1.8** - building.go 硬编码日期过滤改为相对当前日期动态计算
6. **1.9** - tasks.go 退租时同步结束对应合同
7. **1.10** - rooms.go 签约前检查并结束旧合同
8. **1.12** - buildings.go GetRooms 添加 status/floor/layout 过滤
9. **1.13** - 多个 List 方法查询失败返回 500 错误码
10. **1.14** - 统一账单编号生成，移除 utils.GenerateBillNo
11. **1.15** - Building List 改为批量聚合查询消除 N+1
12. **1.16** - 金额字段添加 `binding:"gte=0"` 校验
13. **1.17** - 生产环境跳过 AutoMigrate
14. **2.1.1** - JWT 刷新令牌吊销机制（内存黑名单 + 清理协程）
15. **2.1.2** - CORS 支持环境变量配置
16. **2.1.4** - 全局 RateLimit 中间件 60 req/min
17. **2.3.2** - handleDepositRefund 返回值检查并回滚
18. **2.3.3** - 日志文件打开失败仅警告不阻止启动
19. **2.3.5** - 响应数据解包增加 null 检查
20. **2.4.1** - 限流器统一使用 Mutex + map
21. **2.5.1** - routes.go Service 对象共享实例
22. **2.6.1** - GetTrend 改为单条 GROUP BY 查询
23. **2.6.2** - 租金账单创建改用 JOIN 查询
24. **2.6.3** - 到期检查添加 LIMIT 100
25. **2.8.1** - buildings/bills/rooms 列表添加分页支持
26. **2.8.2** - 删除房间前检查活跃合同
27. **2.8.3** - 添加统一业务错误码（CodeSuccess/CodeMonthSettled 等）
28. **2.9.1** - CleanupSoftDeleted 每日清理 90 天前软删除数据
29. **2.9.2** - 外键添加 OnDelete:CASCADE/SET NULL 约束
30. **2.9.3** - GetWithStats 改为单条聚合查询
31. **2.10.1** - 全部 handler 统一使用 utils.GetBuildingID()
32. **2.10.2** - 提取 validateUploadFile + uploadToStorage 公共函数
33. **低危1** - Districts 改为使用 shenzhen.js 本地数据
34. **低危5** - 更新房间状态拒绝无效值
35. **低危6** - seedAdmin 函数同步修复
36. **低危10** - 日期查询改用日期范围替代 LIKE/DATE_FORMAT
37. **低危7** - 公开接口不再加载租户 PII，添加 GetActiveContractPublic 方法
38. **低危8** - SetTime 添加 user_id 审计日志
39. **低危9** - 数据库连接池添加 ConnMaxIdleTime 配置
40. **2.5.3** - MediaHandler 改为使用 MediaService 方法
41. **2.10.2** - MediaService 添加 GetRoomByID、GetBuildingByID、CreateMedia、DeleteMedia、GetMediaByID、UpdateBuildingCover 方法

### 前端已修复（49 个）
1. **1.4** - BuildingPublicRoom.vue 移除租客姓名和手机号展示
2. **1.5** - BuildingPublic.vue 房东手机号使用 maskPhone 脱敏
3. **1.11** - AdminBuildings.vue 和 AdminBuildingDialogs.vue 复制登录链接改为 /login
4. **1.18** - BuildingPublic.vue 和 BuildingPublicRoom.vue 改为从 utils/format.js 导入
5. **2.1.6** - router/index.js 路由守卫改为使用 authStore
6. **2.10.3** - useUtils.js 改为从 format.js 导入共享函数
7. **2.10.4** - utils/format.js statusTagType 统一各处实现
8. **2.10.5** - 楼层/户型选项提取到 utils/constants.js
9. **2.11.1** - stores/auth.js 添加 storage 事件监听多标签页同步
10. **2.11.2** - api/index.js 401 处理改为调用 authStore.logout()
11. **2.11.3** - BuildingLayout.vue 改为使用 authStore
12. **2.12.2** - RoomDetail.vue catch 块添加错误日志
13. **低危5** - RoomSidebar.vue 移除冗余 action 属性
14. **低危6** - BuildingSettings.vue 统一上传响应访问路径
15. **低危7** - RoomDetail.vue catch 块添加错误提示
16. **低危8** - BillDialog.vue 金额最小值改为 0.01
17. **低危9** - LandlordApply.vue 手机号正则改为 11 位大陆手机号
18. **低危10** - ECharts 重复注册提取到 utils/echarts.js
19. **前端1** - 移动端检测逻辑提取 useMobile composable
20. **前端2** - MobileUserMenu 组件提取，两处布局复用
21. **前端12** - 添加 ESLint 配置文件和 npm scripts
22. **前端3** - AdminLayout.vue 内联样式改为 CSS 类
23. **前端3** - BuildingSettings.vue 内联样式改为 CSS 类
24. **前端11** - Login.vue 内联样式改为 CSS 类

---

## 五、未修复问题清单（6 个）

> 以下为当前仍未修复的问题，按优先级排列

### 高危（1 个）- 架构变更较大，暂不修复
| # | 问题 | 说明 |
|---|------|------|
| 1.6 | Token 存 localStorage 易被 XSS 窃取 | 需改为 httpOnly Cookie，涉及前后端架构变更 |

### 中危（4 个）- 需要评估修复成本
| # | 问题 | 说明 |
|---|------|------|
| 2.5.2 | 日期字段混用 string 和 time.Time 类型 | 需要统一模型定义，可能影响数据库迁移 |
| 2.12.1 | 无 TypeScript，props 无类型约束 | 需迁移至 TypeScript，工作量极大 |
| 前端3 | 模板大量使用内联样式 | 已修复关键组件，剩余部分需逐步重构 |
| 前端11 | 登录页使用 Vant 组件，桌面端视觉不佳 | 已优化样式，Vant 组件适合移动端 |

### 低危（1 个）- 不影响功能
| # | 问题 | 说明 |
|---|------|------|
| 低危4 | 测试覆盖率极低 | 需编写单元测试和集成测试 |
